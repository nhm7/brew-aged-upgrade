[project]
name = "homebrew-aged-upgrade"
dynamic = ["version"]
requires-python = ">=3.9"
dependencies = []

[tool.uv]
package = false

[tool.setuptools]
packages = []
py-modules = []

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
